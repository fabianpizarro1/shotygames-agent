.env
CLAUDE.local.md
.claude/settings.local.json
node_modules/
.DS_Store

# Snapshots del catálogo DROPI: ~13MB cada uno y se regeneran solos con
# `node projects/dropshipping/catalogo.js snapshot`. No van al repo.
projects/dropshipping/data/

# Truquito tiene su propio repo (proyecto de Vercel aparte)
projects/truquito/
projects/avanora/

# Proyectos con su propio repo (se despliegan aparte)
projects/emparejados/
projects/entredados/
projects/dropi-dashboard/

# Caché de pnpm — no va al repo
.pnpm-store/
